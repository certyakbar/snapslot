name: Post-merge manifest reset retired neutralized

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  neutralized-reset:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: true
