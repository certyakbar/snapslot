# SnapSlot Acceptance Ledger

This ledger records verification truth against the Constitution. Statuses are PASS, FAIL, or UNVERIFIED only. Do not mark PASS without canonical repository evidence. Use UNVERIFIED if proof is missing. Do not treat later-phase or planned work as implemented.

**Note**: This ledger is part of canonical governance and records verification truth against the Constitution. Statuses must remain PASS, FAIL, or UNVERIFIED.

| Area | Status | Proof / Notes |
| --- | --- | --- |
| Governance: approval manifest binding (Phase 0.75) | PASS | Manifest model implemented in PR #13 and proven by closed sacrificial proof PR #16: P1 FAIL with verdict=NONE, P2 PASS on valid current manifest, P3 FAIL after post-approval commit, P4 FAIL on BLOCK override, P5 PASS after re-approval. Phase 0.75 proof is complete and PR #16 was closed without merge per plan. See docs/SNAPSLOT_SENTINEL_CONTRACT.md section 12. |
| Business registration | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): `POST /api/signup` returns 201 for two independent businesses, `businessId` prefixed `biz_`, `adminUrl` equals `/admin/{businessId}`, session cookie set on response. |
| Login | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): `POST /api/login` with valid credentials returns 200, `businessId` matches signup value, `adminUrl` equals `/admin/{businessId}`, session cookie established and confirmed active via `/api/session`. |
| Logout | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): `POST /api/logout` returns 200; subsequent `GET /api/session` with the same cookie returns 401, confirming session destroyed. |
| Session protection | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): unauthenticated `GET /api/session` returns 401; unauthenticated `GET /api/business/:id` returns 401; unauthenticated `GET /admin/:businessId` returns 302 redirect to login page. |
| Admin route protection | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): unauthenticated `/admin/:businessId` returns 302 to `/login?redirect=...`; authenticated session for business B accessing business A's admin URL returns 302 to B's own admin URL, enforcing ownership. |
| Redirect behaviour | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): (1) unauthenticated admin request â†’ 302 to `/login?redirect=%2Fadmin%2F{businessId}`; (2) mismatched-session admin request â†’ 302 to own admin URL; (3) authenticated `GET /login` â†’ 302 to own admin URL. |
| Password reset | PASS | Proven by `tests/passwordReset.test.ts` (commit `87910df`, `PASS 3 tests` on `origin/main`): `POST /api/request-password-reset` returns 200 with identical body for known and unknown email addresses (enumeration prevention); SMTP-captured email contains correct `To:`, `Subject: Reset your SnapSlot password`, and a 64-hex token; `POST /api/reset-password` with valid token updates the password, invalidates all existing sessions (401), rejects old password (401), accepts new password login (200 with correct `businessId`), and rejects token reuse (400 `"Invalid or expired reset token."`); short password rejected (400 `"Password must be at least 8 characters."`); email delivery failure returns safe generic 200. `sendPasswordResetEmail` transport injection proven. Resolves C-001. |
| Business account isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): session A accessing `GET /api/business/{businessIdB}` returns 403; session A businessId confirmed unchanged after business B creation via `/api/session`. |
| Service isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): A's service list includes A's service ID and excludes B's; B's service list includes B's service ID; cross-tenant service `GET` and `POST` with session A against business B both return 403. |
| Booking isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): A's booking list includes A's booking ID and excludes B's; cross-tenant booking `GET`, `POST`, and cancel `PATCH` with session A against business B all return 403. |
| Route isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): cross-tenant 403 verified for all route families â€” account `GET`, services `GET`/`POST`, availability `GET`/`PUT`, blocked-times `GET`/`POST`/`PATCH`/`DELETE`, slots `GET`, bookings `GET`/`POST`/cancel `PATCH`. |
| Session isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): session A confirmed bound to business A after business B creation; all cross-tenant API calls with session A return 403; post-logout session returns 401; fresh login creates independent session confirmed via `/api/session`. |
| Slug isolation | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): public booking pages for slug A and slug B each return correct distinct business; slug A's public services exclude B's service ID; duplicate slug registration rejected with 400 and message matching `/slug is already taken/i`. |
| No cross-business leakage | PASS | Proven by `tests/authIsolation.test.ts` (commit `c3af8dff`, `PASS 1 tests` on `origin/main`): 403 returned for all cross-tenant access attempts across account, services, availability, blocked-times, slots, and bookings routes; A's booking list excludes B's booking ID; public slug A's service list excludes B's service ID; session boundaries enforce correct business scope throughout. |
| Service create/list | PASS | Proven on `origin/main` by `tests/coreProof.test.ts` (`PASS 8 tests`) test `service create persists all fields and list returns them`, which verifies `BookingStore.createService` and `BookingStore.listServices`; route-level create/list is also proven by `tests/authIsolation.test.ts` (`PASS 1 tests`) through authenticated `POST /api/business/:businessId/services` and `GET /api/business/:businessId/services`. |
| Service edit/remove/activate/deactivate | PASS | Proven by `tests/coreProof.test.ts` (commit `3548da6`, `PASS 8 tests` on `origin/main`): test `service lifecycle route supports edit, activate/deactivate, and remove` verifies all four operations via authenticated routes. Previously unverified only because the test runner was not executing coreProof â€” fixed in commit `3548da6`. |
| Weekly availability | PASS | Proven on `origin/main` by `tests/coreProof.test.ts` (`PASS 8 tests`) test `weekly availability round-trip: updateWeeklyAvailability persists and listWeeklyAvailability reads back correctly`, which verifies persistence and retrieval of weekly availability windows; authenticated `PUT /api/business/:businessId/availability` is also exercised in `tests/authIsolation.test.ts` (`PASS 1 tests`). |
| Multiple windows per day | PASS | Proven on `origin/main` by `tests/coreProof.test.ts` (`PASS 8 tests`) test `multiple non-overlapping windows on same day are stored and generate slots in each window`, which stores two same-day windows and verifies slot generation in both; this is corroborated by `tests/bookingEngine.test.ts` test `generateAvailableSlots uses the business timezone and supports multiple windows in one day` (`PASS 11 tests`). |
| Blocked times create/edit/remove | PASS | Proven on `origin/main` by `tests/coreProof.test.ts` (`PASS 8 tests`) test `blocked time create, edit, and remove lifecycle`, which verifies `createBlockedTime`, `updateBlockedTime`, `deleteBlockedTime`, and `listBlockedTimesForDate` across the full lifecycle. |
| Public booking link | PASS | Proven on `origin/main` by `tests/coreProof.test.ts` (`PASS 8 tests`) test `public booking page route serves slug page and cancel API marks booking cancelled in response and listing`, which verifies `GET /booking/:slug` returns 200 and serves HTML for a real signed-up business slug; public slug resolution is also proven by `tests/authIsolation.test.ts` (`PASS 1 tests`) via `GET /api/booking-page/:slug`. |
| QR code / booking link isolation | PASS | Proven by commit `61b7583` on `origin/main`: `GET /api/business/:businessId/qr` is protected by `assertBusinessSession` â€” only the authenticated business owner can fetch their own QR code. The QR encodes `/booking/:slug` which is already slug-isolated per business. Admin dashboard displays QR image and booking link loaded via authenticated fetch. No cross-tenant access possible. |
| Booking creation | PASS | Proven on `origin/main` by booking-engine tests creating bookings via `BookingStore.createBooking` (including accepted future bookings and persisted listing behavior in conflict/concurrency scenarios) in `tests/bookingEngine.test.ts` (`PASS 11 tests`). |
| Booking visibility | PASS | Proven on `origin/main` by `tests/authIsolation.test.ts` (`PASS 1 tests`): authenticated `GET /api/business/:businessId/bookings` for business A includes A's booking ID and excludes business B's booking ID, proving tenant-scoped booking visibility. |
| Booking cancellation | PASS | Proven by T-04-F2 on branch `feat/t-04-f2-cancel-reschedule-closure`: direct typecheck command `node node_modules/typescript/bin/tsc --noEmit` exited 0 with no output; `npm test` exited 0 with exact new cancel proof lines `PASS cancelBooking rejects a booking that is already cancelled`, `PASS cancelBooking rejects a completed booking`, `PASS cancelBooking accepts pending_payment booking`, `PASS cancelBooking accepts rescheduled booking`, and suite line `PASS 19 tests`. Guard proof from packet grep: `832:      // cancelBooking terminal guard: reject cancelled/completed before status assignment.`, `833:      if (booking.status === "cancelled" || booking.status === "completed") {`, `834:        throw new HttpError(400, "Cannot cancel a booking that is already cancelled or completed.");`. UI coverage proof from packet grep: `214:      booking.status === "pending_payment" || booking.status === "confirmed" || booking.status === "rescheduled";`. Full suite proof lines included `PASS 11 tests`, `PASS 1 tests`, `PASS 19 tests`, `PASS 4 tests`, `4 tests: 4 passed, 0 failed`, `Tests:       9 passed, 9 total`, `PASS 15 tests`, `PASS 3 tests`, `PASS 2 tests`, `PASS 3 tests`. C-023 is closed for cancellation guard; C-020 cancel UI coverage is closed. |
| Booking rescheduling | PASS | Proven by T-04-F2 on branch `feat/t-04-f2-cancel-reschedule-closure`: direct typecheck command `node node_modules/typescript/bin/tsc --noEmit` exited 0 with no output; `npm test` exited 0 with `PASS reschedule transitions status and frees the original slot`, `PASS auth/session and tenant-isolation routes enforce boundaries`, suite lines `PASS 19 tests` and `PASS 1 tests`, and full suite lines `PASS 11 tests`, `PASS 1 tests`, `PASS 19 tests`, `PASS 4 tests`, `4 tests: 4 passed, 0 failed`, `Tests:       9 passed, 9 total`, `PASS 15 tests`, `PASS 3 tests`, `PASS 2 tests`, `PASS 3 tests`. API/UI wrapper proof from packet greps: `96:export function rescheduleBooking(businessId, bookingId, requestedStart) {`, `14:  rescheduleBooking as rescheduleBookingRequest,`, `840:    await rescheduleBookingRequest(businessId, bookingId, requestedStart.toISOString());`. Cross-tenant reschedule route is covered in `tests/authIsolation.test.ts` by the same isolation suite. C-020 reschedule UI/API wrapper gap is closed. |
| Slot generation | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` (`generateAvailableSlots uses the business timezone and supports multiple windows in one day`, `blocked times spanning midnight are considered when generating the next day's slots`, and `generateAvailableSlots combines multiple service durations and buffers`) with `PASS 11 tests`. |
| Service duration combination | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `generateAvailableSlots combines multiple service durations and buffers`, asserting combined-service slot start/end outputs (`PASS 11 tests`). |
| Buffer handling | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `generateAvailableSlots combines multiple service durations and buffers`, which verifies buffer-included end times for multi-service bookings (`PASS 11 tests`). |
| Timezone handling | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `generateAvailableSlots uses the business timezone and supports multiple windows in one day`, asserting timezone-correct UTC slot outputs (`PASS 11 tests`). |
| Blocked-time exclusion | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `blocked times spanning midnight are considered when generating the next day's slots`, verifying blocked overlap removal from slot results (`PASS 11 tests`). |
| Existing-booking conflict prevention | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` tests `createBooking rejects a second booking that overlaps an existing confirmed booking` and `conflictsWithExistingTime detects overlapping confirmed bookings and ignores cancelled ones` (`PASS 11 tests`). Boundary remains: near-simultaneous protection is tracked separately as single-process in-memory; cross-process / multi-instance concurrency remains unproven. |
| Past booking rejection | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `createBooking rejects past requestedStart values server-side` (`PASS 11 tests`). |
| Invalid/inactive service rejection | PASS | Proven on `origin/main` by `tests/bookingEngine.test.ts` test `createBooking rejects invalid or inactive services` (`PASS 11 tests`). |
| Near-simultaneous / concurrency double-booking protection | PASS | Proven by pushed `origin/main` patch (`a7f6e0c406c4a52f3693109c082a6994dede32cc`) and passing tests (`PASS 9 tests`); protection covers single-process in-memory concurrency. Cross-process / multi-instance concurrency remains unproven. |
| Shared theme ownership | PASS | Proven by commits `342bfa6` and `a416369` on `origin/main`: all shared layout rules (.page grid/min-height, input/textarea/button width, textarea resize, @media shell breakpoint) extracted from login.html, signup.html, admin.html, and index.html into `public/css/snapslot-theme.css` under scoped body-class selectors. No shared style remains duplicated in page `<style>` blocks. |
| Inline duplicated shared styles | PASS | Proven by commits `342bfa6` and `a416369` on `origin/main`: page-level `<style>` blocks in all four HTML files stripped of all rules now living in `snapslot-theme.css`; only truly page-specific overrides remain inline. |
| Fake or unsupported UI | FAIL | Prior audit proved no fake controls exist in login.html, signup.html, or index.html. T-04-F1 closed the complete-booking UI gap; T-04-F2 closes cancel UI coverage for `pending_payment`, `confirmed`, and `rescheduled` plus admin reschedule UI/API wrapper proof. **Remaining gaps outside T-04-F2 scope:** (1) Admin UI payment action controls remain absent for mark-paid, mark-failed, and refund (C-019). (2) Admin dashboard still has no suspension banner and no disabled-control state for suspended businesses (C-015). This row remains FAIL because those remaining UI gaps are not closed by T-04-F2. |
| UI/data/logic separation | PASS | Proven by audit (Task 7) on `origin/main`: HTML files contain presentation only; JS files in `public/` contain client logic only; `src/` contains all server/data logic. No mixing of concerns across layers. |
| Dashboard professionalism / clarity | PASS | Proven by audit (Task 7) on `origin/main`: admin dashboard sections (Services, Availability, Blocked Times, Bookings) are clearly labelled and map 1:1 to implemented backend features; no confusing or misleading UI elements present. |
| Error handling | PASS | Proven by commit `ecafc8c` on `origin/main`: all routes wrap logic in try/catch and call `handleError`; HttpError instances return their assigned status + message; unexpected errors return 500 with generic message and log internally â€” internal details never leaked. Shared `errors.ts` HttpError class used across server and store layers. |
| Validation coverage | PASS | Proven by commit `ecafc8c` on `origin/main`: all bookingStore.ts domain rules throw HttpError(400) â€” required fields, invalid dates, past times, inactive services, conflict detection, state transition guards. Route layer validates email format, timezone, slug, and all required signup/login fields. Proven via 28 passing tests across all 5 suites. |
| Booking state model | FAIL | Type definitions in `bookingCore.ts` and `Persistence.ts` are correct for discriminated union purposes. **GAPS:** (1) `no_show` appears in `bookingStore.ts` reschedule terminal-state guard by name â€” a deferred state is referenced in live runtime logic (D-006). (2) `partially_refunded` appears in `bookingStore.ts` `allowedTransitions` as a valid payment transition target from `paid`, and in `server.ts` line 773 as an active notification branch â€” the deferred state is a live callable code path, not a type-only declaration (C-004 / D-002 runtime leak). State type model is correct; live transition and notification logic violate the deferred-by-law rule. |
| Notifications | FAIL | Booking confirmed, rescheduled, cancelled, payment required, payment received, and refund issued are proven and wired. **GAPS:** (1) `sendPaymentFailed` function does not exist in `notificationService.ts`. (2) `server.ts` payment route does not trigger any notification when payment status transitions to `failed`. (3) `notificationService.ts` contains a stale comment at lines 65â€“66 stating payment notifications are "NOT IMPLEMENTED â€” payments feature not yet built" â€” this is factually wrong, confuses contributors, and must be removed. Payment-failed event is a Constitution Â§15 current-phase requirement (C-010, C-018). |
| Payments & deposits | FAIL | Backend routes, store logic, and 9 tests proven for payment config, deposit logic, mark_paid, and refund. **GAPS:** (1) `admin-ui.js` has zero payment action controls â€” no mark-paid, no mark-failed, no refund button or form. (2) `api.js` has no payment action function. (3) Payment status and deposit amount are not displayed in the admin bookings table. (4) `mark_failed` payment transition has no booking-status consequence defined in code (booking status does not change when payment â†’ failed). (5) `partially_refunded` is a live callable transition in `allowedTransitions` (deferred feature in runtime logic). (6) Payment-failed notification is not implemented. Backend is proven; the entire UI half and the failed-payment path are absent (C-003, C-019). |
| Subscription & billing | FAIL | 15 subscription tests proven for platform admin billing actions, suspension/reactivation/deactivation/cancellation, and public-route business-status gating. **GAPS:** (1) `active  deactivated` direct transition is blocked by code (deactivation requires `subscriptionStatus === "suspended"` first)  contradicts State Matrix which shows `active  deactivated: YES` (C-022). (2) `pricePerMonth: 60.0` is a hardcoded float literal in server.ts subscription route, not derived from `SUBSCRIPTION_PRICE_PENCE` constant  billing source-of-truth violation (C-021). C-016 (admin write-route status gating) resolved by T-1-C1 (PR #20). Remaining open: C-021, C-022. |
| Suspended business admin write-blocking | PASS | Proven by T-1-C1 (PR #20, merged on main): `assertBusinessActive` added to all 12 admin write routes  services POST/PATCH/DELETE, availability PUT, blocked-times POST/PATCH/DELETE, payment-config PUT, bookings POST, cancel PATCH, reschedule PATCH, payment PATCH. Suspended and deactivated businesses receive 503 on all write routes; read routes (GET services, GET availability, GET blocked-times, GET bookings) remain accessible for suspended businesses (read-only, not offline). Proven by `tests/businessStatusGating.test.ts` PASS 2 tests. Resolves C-016. |
| Booking complete (route and store method) | PASS | `completeBooking` store method exists in `bookingStore.ts`. State guard rejects `pending_payment -> completed` with `HttpError 400`, proven by Test C (`tests/coreProof.test.ts`). State guard rejects `cancelled -> completed` with `HttpError 400`, proven by Test D (`tests/coreProof.test.ts`). State guard rejects `completed -> completed` with `HttpError 400`, proven by Test E (`tests/coreProof.test.ts`). `confirmed -> completed` transition proven by Test A (`tests/coreProof.test.ts`). `rescheduled -> completed` transition proven by Test B (`tests/coreProof.test.ts`). `PATCH /api/business/:businessId/bookings/:bookingId/complete` route exists in `server.ts`, gated by `assertBusinessSession` and `assertBusinessActive`. Route confirmed happy-path proven by Test F (`tests/coreProof.test.ts`). Route `pending_payment` rejection proven by Test G (`tests/coreProof.test.ts`). Admin UI complete button exists in `admin-ui.js` for confirmed and rescheduled bookings; "Not available" fallback appears only when no action is available; cancel button condition is unchanged. Route is session-protected: unauthenticated `PATCH /complete -> 401`, proven by Assertion 1 (`tests/authIsolation.test.ts`). Route is cross-tenant protected: `sessionA` against business B booking -> `403`, proven by Assertion 2 (`tests/authIsolation.test.ts`). No notification sent on complete (by design, per State Matrix). Exact passing test counts from `npm test` output: `PASS 11 tests`, `PASS 1 tests`, `PASS 15 tests`, `PASS 4 tests`, `Tests: 9 passed, 9 total`, `PASS 15 tests`, `PASS 3 tests`, `PASS 2 tests`, `PASS 3 tests` (67 passing tests total). C-017 addressed by this task. |
| Admin UI â€” payment actions (mark paid, mark failed, refund) | FAIL | `admin-ui.js` has no payment action controls. `api.js` has no payment action function. The admin dashboard cannot perform any payment lifecycle action despite all backend routes and store methods existing and being proven by tests. Workflow G is backend-proven; the UI half is entirely absent (C-019). |
| Admin UI â€” reschedule | PASS | Proven by T-04-F2 on branch `feat/t-04-f2-cancel-reschedule-closure`: direct typecheck command `node node_modules/typescript/bin/tsc --noEmit` exited 0 with no output; `npm test` exited 0 with `PASS reschedule transitions status and frees the original slot`, `PASS auth/session and tenant-isolation routes enforce boundaries`, `PASS 19 tests`, and `PASS 1 tests`. Packet grep proof: `96:export function rescheduleBooking(businessId, bookingId, requestedStart) {` in `public/js/api.js`; `14:  rescheduleBooking as rescheduleBookingRequest,` and `840:    await rescheduleBookingRequest(businessId, bookingId, requestedStart.toISOString());` in `public/js/admin-ui.js`; `214:      booking.status === "pending_payment" || booking.status === "confirmed" || booking.status === "rescheduled";` confirms active-state UI coverage. C-020 admin reschedule UI/API wrapper gap is closed. |
| Admin UI â€” suspended read-only enforcement | FAIL | `admin-ui.js` does not check `subscriptionStatus`. No suspension banner is rendered. No controls are disabled or hidden for suspended businesses. Constitution Â§4.3, Architecture Â§7.3 require a clearly read-only admin dashboard for suspended businesses. The UI enforcement is entirely absent (C-015, C-016). |
| Payment-failed notification | FAIL | `sendPaymentFailed` function does not exist in `notificationService.ts`. No branch for `failed` in `server.ts` payment route notification logic. Constitution Â§15, Architecture Â§14.1 require payment-failed as a current-phase event notification (C-010, C-018). |
| Platform owner password security | PASS | PR #14 is merged on main. Owner login no longer uses plain env-var equality: `SNAPSLOT_ADMIN_PASSWORD` is treated as a `salt:hash` credential, and verification reuses the business-auth primitive path through `verifyPassword` (scrypt hashing with salt plus timingSafeEqual comparison). This resolves C-024 without claiming closure of unrelated Phase 1 auth/status gaps. |
| Deactivated business admin login blocking | PASS | Proven by T-1-B1 (PR #19, merged on main): `POST /api/login` now checks `business.subscriptionStatus === "deactivated"` after password verification and throws HttpError(403, "Your account has been deactivated. Please contact support.") before session creation. Suspended businesses remain login-permitted (Constitution 4.3). Proven by `tests/deactivatedLogin.test.ts` PASS 3 tests. Resolves C-025 (login-route portion). C-008 (deactivated admin-UI behavior normalization) remains open under Phase 7. |
| Payment history | UNVERIFIED | Constitution Â§ Billing history; later-phase, pending implementation proof. |
| Add-ons | UNVERIFIED | Later-phase in Constitution; pending implementation proof or confirmed absence. |
| Definition of done evidence | UNVERIFIED | Requires proof that backend, frontend, validation, errors, isolation, tests, docs meet criteria. |
| Deployment and process management | UNVERIFIED | No deployment docs, no process manager configuration, no environment variable documentation. Not accounted for in any planning document. Required before production operation. |
| Security hardening (headers, body limits, CSRF, HTTPS) | UNVERIFIED | No security headers middleware. No body size limits on express.json(). No CSRF token protection beyond SameSite=Lax. No HTTPS enforcement or redirect. These are production-required controls not currently planned or implemented. |
| GDPR and privacy compliance | UNVERIFIED | `gdprRetentionFlaggedAt` is set on deactivation but no deletion job, export route, or retention policy exists. No privacy policy page, no cookie consent, no terms of service. Legally required at first user contact. Not accounted for in any planning document. |
| Monitoring and observability | UNVERIFIED | Only a `/health` endpoint exists. No structured logging, no error tracking, no metrics, no alerting. `console.error` is the only error visibility path. Not accounted for in any planning document. |
| Honest documentation truth | FAIL | README correction is proven and remains accurate. **GAP:** `notificationService.ts` lines 65â€“66 contain a stale comment stating "NOT IMPLEMENTED â€” payments feature not yet built. Placeholders reserved for future Task 6 integration." Payment notifications (`sendPaymentRequired`, `sendPaymentReceived`, `sendRefundIssued`) are fully implemented and wired in `server.ts`. The comment is factually wrong, has survived multiple commits unnoticed, and must be removed. Constitution Â§17 requires docs to match reality. |
| Phase 1 exit criteria | PASS | Governor audit T-1-F1 (2026-04-09): all Phase 1 behavioral exit criteria confirmed met against origin/main proof. T-1-B1 (PR #19) deactivated login block proven by tests/deactivatedLogin.test.ts PASS 3. T-1-C1 (PR #20) all 12 admin write routes gated, proven by tests/businessStatusGating.test.ts PASS 2. T-1-A1 (PR #21) session boundary isolation proven by tests/sessionBoundary.test.ts PASS 4. T-1-D1 (PR #24) password reset proven by tests/passwordReset.test.ts PASS 3. C-024 (PR #14) owner password hashed. Phase 1 status: COMPLETE. |
| Governance: automated intake lane — /newtask logic (code-verified) | PASS | Logic verified from `.github/workflows/create-task-packet.yml` on origin/main (T-GOV-9 PR #45, T-GOV-11 PR #49): `collectValidationFailures()` validates all required fields, `failClosed()` prevents issue creation on malformed input, author gate exists on the `issue_comment` path, and the `workflow_dispatch` path is author-gated to `certyakbar`. |
| Governance: automated intake lane — /newtask issue-comment live execution | PASS | GitHub Actions `Create Task Packet` run `24730966853` on `origin/main` was triggered by `issue_comment` on 2026-04-21 at `2026-04-21T15:25:12Z` and completed with conclusion `success`. Display title: "T-GOV-13 proof: /newtask intake trigger". The `/newtask` issue-comment intake path is witnessed working. |
| Governance: automated intake lane — workflow_dispatch live execution | PASS | GitHub Actions `Create Task Packet` workflow run `#2` was manually triggered on origin/main on 2026-04-16 and completed with status Success. The `workflow_dispatch` intake path is proven working. |
| Governance: dispatch lane logic (code-verified) | PASS | Logic verified from `.github/workflows/dispatch-agent.yml` and `scripts/dispatch-agent.js` on origin/main (T-GOV-10 PR #47): outer gate checks author, issue state, title prefix, and label; `compile-task.js` validates the packet and builds the executor materials; the script performs the Copilot preflight GraphQL check and agent-assignment POST. Live execution proof is UNVERIFIED and recorded separately. |
| Governance: dispatch lane — live execution | UNVERIFIED | Run `24730998040` on 2026-04-21 reached and passed command guard, task-packet issue validation for issue #53, `dispatch-agent.js` invocation, `compile-task.js`, `task_id` parse, and GraphQL preflight execution. The run failed because `copilot-swe-agent` was not present in `suggestedActors`; T-GOV-14 Copilot availability check returned `{"data":{"repository":{"suggestedActors":{"nodes":[{"login":"certyakbar"}]}}}}`. Remaining unproven steps are `gh issue view` issue-state check and `gh api POST /assignees` Copilot assignment. Do not mark PASS without a witnessed successful run and confirmed Copilot assignment. |
| Governance: malformed-packet fail-closed (code-verified) | PASS | Logic verified from `collectValidationFailures()` in `.github/workflows/create-task-packet.yml`: all required string fields are checked, the `risk_level` enum is enforced, the `phase` enum is enforced, the workflow pattern is enforced, array fields are validated, and the `task_id` regex is enforced. `failClosed()` calls `core.setFailed()` with field-level detail, and no issue is created on any validation failure. |
| Governance: Governor operator runbook | PASS | `docs/SNAPSLOT_GOV_RUNBOOK.md` created by T-GOV-12: it covers intake paths, the dispatch path, fail-closed behavior, Sentinel enforcement reference, Governor verdict workflow, the manual fallback path, and an explicit UNPROVEN/BLOCKED item list. |
| Governance: T-GOV-17 Stages 1-9 witness | PASS | PR #64 and `docs/SNAPSLOT_LOOP_RUNNER_WITNESS.md` prove the T-GOV-17 Stages 1-9 witness only. Completion signal: STAGES 1-9 COMPLETE. Proof bundle at proof/. This row remains historical truth for the T-GOV-17 proof boundary; later Stages 1-12 witness truth is recorded separately after T-GOV-24 merge. Full autonomous review, approval, and merge are not proven. Product Phase 5 remains paused. |
| Governance: T-GOV-24 Stages 1-12 witness | PASS | T-GOV-25 records full Stages 1-12 witness truth after T-GOV-24 merge. PR #71 is the merged witness PR. Exact completion signal: STAGES 1-12 COMPLETE. Proof bundle at proof/. Operator/Governor review required before ready or merge. The witnessed run created a draft PR and printed the Stage 12 handoff. The proof bundle covered Stage 1 repo re-anchor, Stage 2 no-key preflight, Stage 3 Governor output, Stage 4 clearance gate, Stage 5 Codex execution, Stage 6 scope verification, Stage 7 command checks, Stage 8 deterministic check handling, Stage 9 proof collection, Stage 10 PR body/proof draft, Stage 11 draft PR creation, and Stage 12 handoff stop. The witness changed only `docs/SNAPSLOT_LOOP_RUNNER_WITNESS.md`. Sentinel repository validation, task ID, risk, file declaration, ledger declaration, protected-path alignment, and declared-scope checks passed for PR #71. Governor final review approved PR #71 before merge; operator remained final merge authority. No approval, ready, merge, manifest edit, Sentinel bypass, ledger update, or product work was automated by the runner. `ops/GOVERNOR_APPROVAL.json` was reset to verdict NONE after PR #71 merge. T-GOV-26 was deferred at that point; see subsequent T-GOV-26 rows for current truth. Token/test-efficiency governance beyond local offline token-budget gates is not claimed solved. Product Phase 5 remains paused. |
| Governance: manual task packet fallback path | PASS | `.github/ISSUE_TEMPLATE/01-task-packet.yml` is verified present on origin/main: it is a structured form with the required fields, auto-applies the `task-packet` label, and pre-fills the `TASK PACKET:` title prefix. It is functional without any workflow or secret dependency. |
| Governance: T-GOV-26 token-budget enforcement — implementation and executable-mode fix | PASS | PR #73 proves local offline token-budget gates for T-GOV-26. PR #74 proves the executable-mode correction. The enforcement is local offline only: no API calls, no network calls, no product files changed. T-GOV-27, T-GOV-28, and T-GOV-29 remain deferred. Product Phase 5 remains paused. |
| Governance: T-GOV-26 token-budget enforcement — live witness | PASS | PR #75 and `docs/SNAPSLOT_TGOV26_TOKEN_BUDGET_WITNESS.md` are the canonical witness evidence because proof/ is gitignored and transient. Stage 3: profile `governor_claude_pro_default`, measured_tokens 1867, hard_limit_tokens 120000, status PASS, api_calls NONE, network_calls NONE, timestamp 2026-04-30T12:32:23Z. Stage 5: profile `builder_codex_default`, measured_tokens 1758, hard_limit_tokens 300000, status PASS, api_calls NONE, network_calls NONE, timestamp 2026-04-30T12:33:40Z. Both stages pass within soft and hard limits. The witness is local offline only. T-GOV-27, T-GOV-28, and T-GOV-29 remain deferred. Product Phase 5 remains paused. |
